# Solana RPC endpoint (defaults to mainnet-beta if omitted)
SOLANA_RPC_URL=https://api.mainnet-beta.solana.com

# Optional pre-flight configuration thresholds
# RUG_SCORE_THRESHOLD=40
# MEV_SCORE_THRESHOLD=50
